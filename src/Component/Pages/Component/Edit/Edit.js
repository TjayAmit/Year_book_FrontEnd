import { MdEdit } from 'react-icons/md';
import { IconButton } from '../../../Packages';

export const Edit = () => {
  return (
    <>
      <IconButton color={'green.400'} onClick={() => {}}>
        <MdEdit />
      </IconButton>
    </>
  );
};

export default Edit;
